package com.coworking.billing.model.enums;

/**
 * Перелік типів робочих просторів, що використовується для визначення тарифу.
 * DESK — індивідуальне робоче місце.
 * MEETING_ROOM — переговорна кімната.
 */
public enum WorkspaceType {
    DESK,
    MEETING_ROOM,
    LARGE_MEETING_ROOM
}
